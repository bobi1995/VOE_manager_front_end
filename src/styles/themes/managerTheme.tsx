import { createTheme } from '@mui/material';

/* have yet to figure out how to create styling for custom classes and not for predefined MUI styles */
export const managerTheme = createTheme({
  palette: {
    /* background: {
      default: 'rgb(249, 250, 251)',
    }, */
    /* primary: {
      main: '#818181',
    }, */
  },
});
